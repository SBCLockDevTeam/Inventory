<?php
// Copy this file to config/secrets.php and fill in real values.
// config/secrets.php is excluded from Git via .gitignore — never commit real credentials.

DB_HOST = localhost
DB_NAME = SBCInv
DB_USER = SBCInv
DB_PASS = 'your-password-here'
